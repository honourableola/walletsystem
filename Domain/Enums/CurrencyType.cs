namespace Domain.Enums
{
    //Retained as enum to reduce complexity, 
    //Can be converted to entity to support update of conversion rate
    public enum CurrencyType
    {
        Naira,
        Dollar = 1450,
        Euro = 1500
    }
}
